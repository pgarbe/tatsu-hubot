machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker build -t pgarbe/tatsu-hubot .

test:
  override:
    - docker run -d pgarbe/tatsu-hubot

deployment:
  hub:
    branch: master
    commands:
      - $DEPLOY
