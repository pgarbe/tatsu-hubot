machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker build -t pgarbe/tatsu-hubot .

test:
  override:
    - docker run -d pgarbe/tatsu-hubot

#deployment:
#  hub:
#    branch: master
#    commands:
#      - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASS
#      - docker push pgarbe/tatsu-hubot
